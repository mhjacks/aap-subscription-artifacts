---
# Install this collection (editable/local) plus its dependencies:
#   ansible-galaxy collection install -r requirements.yml -p ./collections
#   ansible-galaxy collection install -f -p ./collections .
collections:
  - name: theforeman.foreman
    version: ">=3.8.0"
  - name: .
    type: dir
