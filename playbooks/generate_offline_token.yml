---
- name: Generate Red Hat offline token for AAP artifacts
  hosts: localhost
  connection: local
  gather_facts: false
  roles:
    - role: aap_subscription_artifacts
      vars:
        aap_artifacts_action: generate
